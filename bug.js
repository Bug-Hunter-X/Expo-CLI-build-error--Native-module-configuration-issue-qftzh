This error occurs when using Expo CLI with a project that has a dependency on a native module that is not properly configured for both iOS and Android platforms.  The error message itself can vary, but often mentions issues with linking native modules or missing required files.